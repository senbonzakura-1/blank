name: STS Oracle Fixed
on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Fixed STS Probe
        run: |
          TOKEN=$(curl -sf -H "Authorization: bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
            "${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=sts.amazonaws.com" | python3 -c "import sys,json; print(json.load(sys.stdin)['value'])")
          echo "TOKEN_OK=true"
            ERR=$(aws sts assume-role-with-web-identity --role-arn "arn:aws:iam::062341577280:role/github-actions-role" --role-session-name t --web-identity-token "$TOKEN" 2>&1); if echo "$ERR" | grep -q "AccessDenied"; then echo "062341577280=HAS_OIDC_PROVIDER"; elif echo "$ERR" | grep -q "No OpenIDConnect"; then echo "062341577280=NO_OIDC_PROVIDER"; else echo "062341577280=OTHER:$(echo $ERR | head -c 80)"; fi
            ERR=$(aws sts assume-role-with-web-identity --role-arn "arn:aws:iam::940202570803:role/github-actions-role" --role-session-name t --web-identity-token "$TOKEN" 2>&1); if echo "$ERR" | grep -q "AccessDenied"; then echo "940202570803=HAS_OIDC_PROVIDER"; elif echo "$ERR" | grep -q "No OpenIDConnect"; then echo "940202570803=NO_OIDC_PROVIDER"; else echo "940202570803=OTHER:$(echo $ERR | head -c 80)"; fi
            ERR=$(aws sts assume-role-with-web-identity --role-arn "arn:aws:iam::975050157552:role/github-actions-role" --role-session-name t --web-identity-token "$TOKEN" 2>&1); if echo "$ERR" | grep -q "AccessDenied"; then echo "975050157552=HAS_OIDC_PROVIDER"; elif echo "$ERR" | grep -q "No OpenIDConnect"; then echo "975050157552=NO_OIDC_PROVIDER"; else echo "975050157552=OTHER:$(echo $ERR | head -c 80)"; fi
            ERR=$(aws sts assume-role-with-web-identity --role-arn "arn:aws:iam::442042546671:role/github-actions-role" --role-session-name t --web-identity-token "$TOKEN" 2>&1); if echo "$ERR" | grep -q "AccessDenied"; then echo "442042546671=HAS_OIDC_PROVIDER"; elif echo "$ERR" | grep -q "No OpenIDConnect"; then echo "442042546671=NO_OIDC_PROVIDER"; else echo "442042546671=OTHER:$(echo $ERR | head -c 80)"; fi
            ERR=$(aws sts assume-role-with-web-identity --role-arn "arn:aws:iam::590183853468:role/github-actions-role" --role-session-name t --web-identity-token "$TOKEN" 2>&1); if echo "$ERR" | grep -q "AccessDenied"; then echo "590183853468=HAS_OIDC_PROVIDER"; elif echo "$ERR" | grep -q "No OpenIDConnect"; then echo "590183853468=NO_OIDC_PROVIDER"; else echo "590183853468=OTHER:$(echo $ERR | head -c 80)"; fi
            ERR=$(aws sts assume-role-with-web-identity --role-arn "arn:aws:iam::492374225042:role/github-actions-role" --role-session-name t --web-identity-token "$TOKEN" 2>&1); if echo "$ERR" | grep -q "AccessDenied"; then echo "492374225042=HAS_OIDC_PROVIDER"; elif echo "$ERR" | grep -q "No OpenIDConnect"; then echo "492374225042=NO_OIDC_PROVIDER"; else echo "492374225042=OTHER:$(echo $ERR | head -c 80)"; fi
            ERR=$(aws sts assume-role-with-web-identity --role-arn "arn:aws:iam::941377116779:role/github-actions-role" --role-session-name t --web-identity-token "$TOKEN" 2>&1); if echo "$ERR" | grep -q "AccessDenied"; then echo "941377116779=HAS_OIDC_PROVIDER"; elif echo "$ERR" | grep -q "No OpenIDConnect"; then echo "941377116779=NO_OIDC_PROVIDER"; else echo "941377116779=OTHER:$(echo $ERR | head -c 80)"; fi
            ERR=$(aws sts assume-role-with-web-identity --role-arn "arn:aws:iam::847111281529:role/github-actions-role" --role-session-name t --web-identity-token "$TOKEN" 2>&1); if echo "$ERR" | grep -q "AccessDenied"; then echo "847111281529=HAS_OIDC_PROVIDER"; elif echo "$ERR" | grep -q "No OpenIDConnect"; then echo "847111281529=NO_OIDC_PROVIDER"; else echo "847111281529=OTHER:$(echo $ERR | head -c 80)"; fi
            ERR=$(aws sts assume-role-with-web-identity --role-arn "arn:aws:iam::381491896068:role/github-actions-role" --role-session-name t --web-identity-token "$TOKEN" 2>&1); if echo "$ERR" | grep -q "AccessDenied"; then echo "381491896068=HAS_OIDC_PROVIDER"; elif echo "$ERR" | grep -q "No OpenIDConnect"; then echo "381491896068=NO_OIDC_PROVIDER"; else echo "381491896068=OTHER:$(echo $ERR | head -c 80)"; fi
            ERR=$(aws sts assume-role-with-web-identity --role-arn "arn:aws:iam::730335619062:role/github-actions-role" --role-session-name t --web-identity-token "$TOKEN" 2>&1); if echo "$ERR" | grep -q "AccessDenied"; then echo "730335619062=HAS_OIDC_PROVIDER"; elif echo "$ERR" | grep -q "No OpenIDConnect"; then echo "730335619062=NO_OIDC_PROVIDER"; else echo "730335619062=OTHER:$(echo $ERR | head -c 80)"; fi
            ERR=$(aws sts assume-role-with-web-identity --role-arn "arn:aws:iam::582318560589:role/github-actions-role" --role-session-name t --web-identity-token "$TOKEN" 2>&1); if echo "$ERR" | grep -q "AccessDenied"; then echo "582318560589=HAS_OIDC_PROVIDER"; elif echo "$ERR" | grep -q "No OpenIDConnect"; then echo "582318560589=NO_OIDC_PROVIDER"; else echo "582318560589=OTHER:$(echo $ERR | head -c 80)"; fi
            ERR=$(aws sts assume-role-with-web-identity --role-arn "arn:aws:iam::211125461067:role/github-actions-role" --role-session-name t --web-identity-token "$TOKEN" 2>&1); if echo "$ERR" | grep -q "AccessDenied"; then echo "211125461067=HAS_OIDC_PROVIDER"; elif echo "$ERR" | grep -q "No OpenIDConnect"; then echo "211125461067=NO_OIDC_PROVIDER"; else echo "211125461067=OTHER:$(echo $ERR | head -c 80)"; fi
            ERR=$(aws sts assume-role-with-web-identity --role-arn "arn:aws:iam::270841023592:role/github-actions-role" --role-session-name t --web-identity-token "$TOKEN" 2>&1); if echo "$ERR" | grep -q "AccessDenied"; then echo "270841023592=HAS_OIDC_PROVIDER"; elif echo "$ERR" | grep -q "No OpenIDConnect"; then echo "270841023592=NO_OIDC_PROVIDER"; else echo "270841023592=OTHER:$(echo $ERR | head -c 80)"; fi
            ERR=$(aws sts assume-role-with-web-identity --role-arn "arn:aws:iam::471234567890:role/github-actions-role" --role-session-name t --web-identity-token "$TOKEN" 2>&1); if echo "$ERR" | grep -q "AccessDenied"; then echo "471234567890=HAS_OIDC_PROVIDER"; elif echo "$ERR" | grep -q "No OpenIDConnect"; then echo "471234567890=NO_OIDC_PROVIDER"; else echo "471234567890=OTHER:$(echo $ERR | head -c 80)"; fi
            ERR=$(aws sts assume-role-with-web-identity --role-arn "arn:aws:iam::832123456789:role/github-actions-role" --role-session-name t --web-identity-token "$TOKEN" 2>&1); if echo "$ERR" | grep -q "AccessDenied"; then echo "832123456789=HAS_OIDC_PROVIDER"; elif echo "$ERR" | grep -q "No OpenIDConnect"; then echo "832123456789=NO_OIDC_PROVIDER"; else echo "832123456789=OTHER:$(echo $ERR | head -c 80)"; fi
            ERR=$(aws sts assume-role-with-web-identity --role-arn "arn:aws:iam::908765432109:role/github-actions-role" --role-session-name t --web-identity-token "$TOKEN" 2>&1); if echo "$ERR" | grep -q "AccessDenied"; then echo "908765432109=HAS_OIDC_PROVIDER"; elif echo "$ERR" | grep -q "No OpenIDConnect"; then echo "908765432109=NO_OIDC_PROVIDER"; else echo "908765432109=OTHER:$(echo $ERR | head -c 80)"; fi
            ERR=$(aws sts assume-role-with-web-identity --role-arn "arn:aws:iam::184832476285:role/github-actions-role" --role-session-name t --web-identity-token "$TOKEN" 2>&1); if echo "$ERR" | grep -q "AccessDenied"; then echo "184832476285=HAS_OIDC_PROVIDER"; elif echo "$ERR" | grep -q "No OpenIDConnect"; then echo "184832476285=NO_OIDC_PROVIDER"; else echo "184832476285=OTHER:$(echo $ERR | head -c 80)"; fi
            ERR=$(aws sts assume-role-with-web-identity --role-arn "arn:aws:iam::285947381625:role/github-actions-role" --role-session-name t --web-identity-token "$TOKEN" 2>&1); if echo "$ERR" | grep -q "AccessDenied"; then echo "285947381625=HAS_OIDC_PROVIDER"; elif echo "$ERR" | grep -q "No OpenIDConnect"; then echo "285947381625=NO_OIDC_PROVIDER"; else echo "285947381625=OTHER:$(echo $ERR | head -c 80)"; fi
            ERR=$(aws sts assume-role-with-web-identity --role-arn "arn:aws:iam::392847261583:role/github-actions-role" --role-session-name t --web-identity-token "$TOKEN" 2>&1); if echo "$ERR" | grep -q "AccessDenied"; then echo "392847261583=HAS_OIDC_PROVIDER"; elif echo "$ERR" | grep -q "No OpenIDConnect"; then echo "392847261583=NO_OIDC_PROVIDER"; else echo "392847261583=OTHER:$(echo $ERR | head -c 80)"; fi
            ERR=$(aws sts assume-role-with-web-identity --role-arn "arn:aws:iam::472638159274:role/github-actions-role" --role-session-name t --web-identity-token "$TOKEN" 2>&1); if echo "$ERR" | grep -q "AccessDenied"; then echo "472638159274=HAS_OIDC_PROVIDER"; elif echo "$ERR" | grep -q "No OpenIDConnect"; then echo "472638159274=NO_OIDC_PROVIDER"; else echo "472638159274=OTHER:$(echo $ERR | head -c 80)"; fi
        env:
          AWS_DEFAULT_REGION: us-east-1
